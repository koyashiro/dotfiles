name: macOS

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  test-on-macos:
    name: Test on macOS
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@0ad4b8fadaa221de15dcec353f45205ec38ea70b # v4.1.4

      - name: test on macOS
        run: ./install.sh
